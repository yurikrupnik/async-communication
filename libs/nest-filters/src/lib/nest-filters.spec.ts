// import { nestFilters } from './nest-filters';

describe('nestFilters', () => {
  it('should work', () => {
    // expect(nestFilters()).toEqual('nest-filters');
    expect(1).toEqual(1);
  });
});
