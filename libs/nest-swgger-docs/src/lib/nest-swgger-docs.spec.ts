// import { nestSwggerDocs } from './nest-swgger-docs';

describe('nestSwggerDocs', () => {
  it('should work', () => {
    expect(1).toEqual(1)
    // expect(nestSwggerDocs()).toEqual('nest-swgger-docs');
  });
});
